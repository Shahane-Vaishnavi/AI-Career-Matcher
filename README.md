# AI Career Matcher 🧭

**AI-powered career matching application** built with Vite + React.

---

## 🚀 Quick Start

1. Install dependencies:

```bash
npm install
```

2. Run development server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
```

4. Preview production build locally:

```bash
npm run preview
```

---

## 🔧 Project Structure

- `index.html` — App entry
- `src/` — React app source
  - `App.jsx`
  - `main.jsx`
- `components/` — UI components (e.g., `AICareerMatcher.jsx`)
- `data/` — Static data used by the app (`careerMatches.js`, `skills.js`, `interests.js`, `education.js`, `personality.js`)
- `styles/` — CSS files (e.g., `career.css`)
- `public/` — Static assets

---

## 🧩 Tech Stack

- React
- Vite
- JavaScript (JSX)

---

## 💡 Notes

> The project scripts are defined in `package.json` (`dev`, `build`, `preview`). Ensure Node.js (v16+) is installed.

---

## 🤝 Contributing

Feel free to open issues or send pull requests — include clear descriptions and, if applicable, screenshots or repro steps.

---

## 📄 License

Specify the license used for the project (e.g., MIT) or add a `LICENSE` file.

---

If you want, I can extend the README with screenshots, badges, or a development roadmap. 🔧